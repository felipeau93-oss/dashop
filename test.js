const { createClient } = require(@supabase/supabase-js); const s = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); s.from(penalidades).select(filial).not(filial, in, (SJP,CWB)).limit(5).then(r => console.log(STRING1:, r.error, r.data)); s.from(penalidades).select(filial).not(filial, in, (SJP,São Paulo)).limit(5).then(r => console.log(STRING2:, r.error, r.data)); s.from(penalidades).select(filial).not(filial, in, [SJP,CWB]).limit(5).then(r => console.log(ARRAY:, r.error, r.data));
